#include<stdio.h>

int main(void)
{
    int a, b, c, d, e;

    d = a * a * a + b * b * b + c * c * c;
    e = a * 100 + b * 10 + c;

    for(a = 0; a <= 100; a++)
    {
        for(b = 0; b <= 100; b++)
        {
            for(c = 0; c <= 100; c++)
            {
                
            }
        }
    }
    return 0;
}